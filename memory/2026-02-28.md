# Memory — 2026-02-28

- Timestamp: 2026-02-28 20:57 KST (Asia/Seoul)

- Actions / Decisions:
  - User instructed to STOP work on the Photo Gallery project and pause/halt further development. Assistant should not continue photo gallery tasks until the user re-initiates.
  - User expressed hobby: creating N-행시 using subway station names. They want a new web project to publish these N-행시, with Seoul and Busan subway maps as UI. Key interaction: stations with N-행시 are highlighted (bold) and show the poem in a rollover/click popup. User requested a full project specification and the assistant provided a detailed planning & dev doc.

- New project: "지하철 N행시 (Subway Npoem)"
  - MVP idea: interactive SVG-based subway map (Seoul, Busan), station nodes show N-행시 via popup; admin/moderation, search by station/line/tag, shareable URLs for each poem.
  - Tech suggestions given: Next.js (App Router), Tailwind, PostgreSQL (Neon), Cloudflare R2 for images (if needed), Node/Next API routes, SVG for line maps, exifr/Sharp not necessarily required for this project.

- User preferences / context noted today:
  - Prefers Korean-language UI/content (established earlier).
  - Timezone: Asia/Seoul.
  - Has external drives and local workspace but the assistant cannot access /Volumes paths directly; user must provide files or run commands locally for file operations.

- Misc notes:
  - User performed multiple model switches during the session; assistant should continue interacting normally regardless of model backend changes.

Saved by: assistant (on user request).
---
2026-02-28 22:54 KST
- 작업: 서울/부산 지하철 SVG 지도 원본을 Wikimedia Special:FilePath에서 다운로드하여 작업공간에 저장함.
  - 저장 위치: assets/svg/Seoul_subway_linemap_en.svg, assets/svg/Busan_metro_now.svg
  - 목적: "지하철 N행시 / Subway Npoem" 프로젝트용 자산 수집 및 다음 단계(React 변환, 역 좌표 추출, 간단 데모 페이지)를 준비함.
- 상태: SVG는 저장되었으나 web_fetch 도구의 출력 제한으로 다운로드 로그가 일부 잘림; 전체 파일은 workspace에 저장됨.
- 다음 권장 작업: (사용자 선택)
  1) SVG → React 컴포넌트 변환 (각 역에 id/slug 부여)
  2) SVG에서 역 데이터(이름, 라인, 좌표)를 추출해 CSV로 생성
  3) 간단한 데모 페이지(Next.js)로 마우스오버/클릭 팝업 동작 시연
  4) 각 SVG의 라이선스(Commons 파일 페이지) 요약 및 의무(저작자표시/동일조건 변경허락 등) 문서화
- 메타: 사용자의 언어·UI 선호는 한국어, 타임존 Asia/Seoul (참조용).
