# Memory log — 2026-03-01

- Time: 2026-03-01 14:37 Asia/Seoul
- Actions performed:
  - Expanded seoul-extracted-stations.json to include 120 then 182 matched candidates for verification UI seeding.
  - Integrated Puppeteer render→match pipeline and produced data/seoul-final-stations.json (182 stations) via scripts/run-render-match.js.
  - Generated preferred CSS classes for station text elements and saved to data/preferred-classes.json (classes: st49, st50, st53, st54, T, st55, st56); applied to scripts/extract-stations.js.
  - Attempted automated GTFS acquisition via transit.land; received Unauthorized. Created data/transitland-feeds.json with the response.
  - Implemented multiple GTFS discovery scripts (scripts/find-gtfs-sources.js, scripts/crawl-gtfs-links.js, scripts/search-gtfs-portals.js, scripts/deep-crawl-gtfs.js) and ran them; results saved to data/gtfs-*.json (gtfs-portals-check.json, gtfs-crawl.json, gtfs-search-results.json, gtfs-deep-crawl.json).
  - Created a simple preview SVG from matched stations: public/preview-subway-seoul.svg.
  - Added automation scripts: scripts/run-render-match.js, scripts/auto-tune-classes.js, scripts/generate-simple-svg.js.

- Notes / decisions:
  - Puppeteer-rendered positions are treated as authoritative for coordinate extraction; static jsdom parsing is unreliable for getBBox/visual layout.
  - Preferred approach: seed verification UI with a curated sample (20-50 corrected stations), use that to auto-generate extraction rules, then fetch GTFS/stops.txt for georeferencing.
  - transit.land requests are blocked (Unauthorized); fallback is to search KTDB, data.seoul.go.kr, data.busan.go.kr, or accept a transit.land API key.

- Next recommended steps queued:
  1) User to perform manual verification (20-50 stations) via /subway-demo/verify and save results.
  2) Automate extract-stations.js to use render→match outputs as primary pipeline and produce CSV/JSON for final datasets.
  3) Continue deeper portal crawling for GTFS; if fails, request user-provided API key or dataset links.

