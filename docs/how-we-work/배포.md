# 🚀 배포

> 담당: 릴리스 파이프라인 📦 (비서공주 실행, 탐정요정 검증)

## 목적
안정적인 빌드 및 배포 파이프라인을 통해 최신 버전을 라이브에 제공합니다.

## 핵심 태스크
- **Next.js 정적 빌드 & export**: `output: 'export'` 모드로 `out/` 디렉토리 생성
- **Firebase Hosting 자동 배포**: GitHub Actions 또는 로컬 CLI로 배포
- **캐시 무효화 & 배포 검증**: Cache-Control 헤더로 CDN 캐시 문제 방지

## 배포 파이프라인

```
소스 수정 → git push origin main
         ↓
  [GitHub Actions] (자동)          [로컬 CLI] (수동)
  npm ci → npm run build           npm run build
  → firebase deploy                → npx firebase-tools deploy
         ↓                                  ↓
  Firebase Hosting (324-company-bip)
         ↓
  https://324-company-bip.web.app ✅
```

## 인프라 정보
| 항목 | 값 |
|---|---|
| Firebase 프로젝트 | `project-a3e9a` |
| Hosting 사이트 | `324-company-bip` |
| 배포 디렉토리 | `bip/out/` |
| 라이브 URL | https://324-company-bip.web.app |
| GitHub 리포 | https://github.com/nohdaeyoung/live |
| CI 워크플로우 | `.github/workflows/deploy.yml` |
| 필요 Secret | `GCP_CREDENTIALS` (base64 서비스 계정 JSON) |

## 실제 수행 기록

### 2026-02-23: 배포 파이프라인 구축
- Next.js `output: 'export'` 설정 완료
- `firebase.json` 구성: site `324-company-bip`, public `out`
- Cache-Control 헤더 설정: `/`, `/index.html`에 `no-cache, no-store, must-revalidate`
- GitHub Actions 워크플로우(`deploy.yml`) 추가 — main push 트리거

### 2026-02-23: 다수 배포 수행
- 프로필 이미지 업데이트 → 빌드 → 배포 ✅
- 팀 멤버 카드 활성화 → 빌드 → 배포 ✅
- 문구 변경 (헤딩 텍스트, AI 인원수) → 빌드 → 배포 ✅
- 프로세스 카드 레이아웃 변경 → 빌드 → 배포 ✅
- 태스크 텍스트 업데이트 → 빌드 → 배포 ✅

### 2026-02-23: 긴급 패치 수행
- `out/index.html` 직접 수정 → firebase deploy → 소스 반영 → CI 재배포

## 배포 절차 (수동)
```bash
cd bip
npm run build
npx firebase-tools deploy --only hosting --project project-a3e9a --non-interactive
```

## 운영 규칙
- **배포 승인**: 대영 승인 → 비서공주 배포 실행 → 탐정요정 검증 → 완료 보고
- **긴급 패치**: `out/index.html` 직접 수정 가능하나, 반드시 소스에 동일 변경 반영 후 CI 재배포
- **빌드 산출물**: git에 커밋하지 않음 (CI가 생성)
